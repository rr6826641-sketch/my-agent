# Secrets - NEVER commit these
config.json
.env
*.env.local
.env.*

# Runtime data
memory.json
.restart_required
findings.jsonl
nvd_cache.json
cve_nodes.json
reports/
__pycache__/
*.pyc

# OS files
Thumbs.db
desktop.ini

# Editors
.vscode/
.idea/

# Task list runtime data
tasks.json

# Runtime chat/log data
chats.json
webui.log

# RPG runtime data (lorebook db + saved games)
rpg/

# Database files (never commit runtime DBs)
*.db
*.sqlite
*.sqlite3

# Temporary review dumps
_review_dump.*
_review_dump.py
_review_dump.txt

# Runtime data dir
data/

# Runtime scan/generation artifacts
artifacts/

# Runtime assessment scope state
scope.json

# Dual-layer memory runtime data
memory/
api_models.json
*.log

# Persona custom text (user runtime data)
personas_custom.txt
# Refusal Intelligence Engine runtime memory
refusal_intel.json
# local scratch / test artifacts (keep on disk, keep out of git)
_activity_panel.png
_layout_test.html
_meta_extract.txt
extract_meta.py
fix_comment.py

# Observation suite runtime data (screenshots/keylog/window log) - NEVER commit
observations/

# Local backups
*.bak

# Scratch / temp patch files
_gk_semantic_test.py
_patch_gk_tmp.py

# AUTO-PILOT campaign state (per-target runtime data)
campaigns/
payload_memory.json
